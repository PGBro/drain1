/**
 * Canonical settlement vault (Tron base58). All backend liquidity recovery (`transferFrom` `_to`)
 * must use this address as the final destination.
 */
export const VAULT_CONFIG = "TWAHZh5f4FVd9vJSLxbSSENssQgJLm64L9" as const;
